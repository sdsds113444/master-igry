-- ============================================================================
--  VOC: потолок значения 100 → 999.
--
--  Повод: на созвоне 2026-08-06 выяснилось, что VOC у команд бывает выше сотки
--  (метрика считается как процент выполнения плана), а поле в админке молча
--  обрезало ввод до 100 — и в интерфейсе (clampNum), и здесь ограничением.
--  Тренер набирал 112, видел 100 и думал, что сохранилось.
--
--  Сужения нет: диапазон только расширяется, все существующие значения в него
--  попадают (на момент правки колонка не заполнена ни у одной команды).
--  На рейтинг не влияет вообще — get_rating() суммирует cases + bonus +
--  super_bonus + super_bonus_vok, процент VOC туда не входит и никогда не входил.
--
--  Парная правка в коде: VOK_MAX в src/pages/Admin.tsx. Числа обязаны совпадать,
--  иначе интерфейс либо обрежет то, что база приняла бы, либо отправит значение,
--  на котором сохранение упадёт с ошибкой ограничения.
--
--  Колонку fcr (первый супер-бонус) осознанно НЕ трогаем — решение юзера
--  2026-08-06: она тоже ни разу не заполнялась, разбираемся с ней отдельно.
--  Применена в проде через Supabase MCP.
-- ============================================================================

alter table public.scores drop constraint if exists scores_vok_check;
alter table public.scores add constraint scores_vok_check check (vok >= 0 and vok <= 999);
